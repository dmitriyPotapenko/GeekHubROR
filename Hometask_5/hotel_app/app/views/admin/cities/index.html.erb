<h1> Index Cities </h1>

<%= link_to 'Countries', admin_continent_countries_path(:continent_id) %>
<%= ' | ' %>
<%= link_to 'Home Page', root_path %><br><br>
<% @cities.each do |city| %>
	<%= city.name %>
	<%= link_to 'Show', admin_city_path(city) %>
	<%= link_to 'Edit', edit_admin_city_path(city) %>
	<%end%>
	<br><br>
	<%= will_paginate @cities %>