<h1> New Product </h1>

<%= form_for @product do |f| %>
<%= f.label :name %><br><br>
<%= f.text_field :name %><br><br>
<%= f.collection_select(:category_id, Category.all, :id, :name) %>
<%= f.submit %>
<% end %>
