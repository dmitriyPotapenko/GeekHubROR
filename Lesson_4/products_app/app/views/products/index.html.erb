<h1> Index Product App </h1>

<% @products.each do |product| %>
	<%= product.name %>

	<%= link_to 'Show', product %>
	<%= link_to "Edit", edit_product_path(product) %>
	<%= link_to 'Destroy', product, method: :delete %><br>
<% end %>